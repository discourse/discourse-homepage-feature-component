{{! switches plugin outlet if the setting "above_main_container" is enabled (manual), 
or if "show on" setting is set to "all" (automatic) }}
{{#if
  (or
    (theme-setting "above_main_container") (eq (theme-setting "show_on") "all")
  )
}}
  {{featured-homepage-topics}}
{{/if}}
